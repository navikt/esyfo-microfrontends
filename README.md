# esyfo-microfrontends

# Kjør appen lokalt

1. Installer dependencies: `npm install`
2. Start microfrontends: `npm run dev:<navn-på-microfrontend>`, f.eks: `npm run dev:dialogmote-microfrontend`
3. Appen nås på http://localhost:4321/